server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
  location /api/ {
    proxy_pass ${API_BACKEND_URL};
    proxy_set_header X-Real-IP $remote_addr;
  }
  location /swaggerui/ {
    proxy_pass ${API_BACKEND_URL}/swaggerui/;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
